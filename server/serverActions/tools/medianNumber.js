module.export = (numbers) => {
    numbers = numbers.sort((a,b) => {
        return a - b;
    });

    const isEvenNumber = numbers.length % 2 === 0;

    if (isEvenNumber){

    }
    else {
        
    }
}
